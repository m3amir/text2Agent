# Main configuration file - resources organized into separate files:
# vpc.tf, rds.tf, s3.tf, iam.tf, secrets.tf, bedrock_knowledge_base.tf
# lambda.tf, cognito.tf, terraform.tf, variables.tf, outputs.tf
#
# Configuration files:
# - terraform.tf: Provider configuration (using m3 profile)
# - variables.tf: Variable definitions
# - terraform.tfvars: Variable values
# - outputs.tf: Output definitions 